# ── 后端构建产物（backend 目录下已有详细规则）──
backend/target/

# ── 前端构建产物与依赖 ──
frontend/node_modules/
frontend/dist/
frontend/.npm-cache/
frontend/.vite/

# ── 根目录 npm 缓存 ──
.npm-cache/

# ── 前端敏感配置 ──
frontend/.env.local
frontend/.env.*.local

# ── 操作系统生成文件 ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
*.iml
.vscode/
